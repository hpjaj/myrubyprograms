Because a top level local variable has its own local scope.  
A method definition has its own local scope.  
If the two conflict, the top level local variable takes precedence over the method's scope.